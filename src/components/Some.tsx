export const Some = () => {
    const calculating = <T,>(param: T): T => {
        return param;
    };

    return (
        <>
            {/* 여기에 JSX 코드를 추가하거나 필요에 따라 다른 요소로 변경하세요. */}
        </>
    );
};
